# Звіт перевірки комплекту

## Поточне доповнення 2.1.0 — 2026-09-06

- Додано [п'ять профілів](../profiles/ROLES.md), умовне читання та необов'язкові поля ролей і рев'ю.
- Валідатор підготовленого комплекту: `OK`, 22 документи; тести: 24, `OK`.
  Нові випадки перевіряють відсутній файл профілів, сумісність контексту 2.0, неправильний режим,
  дубль поля, обов'язкове рев'ю без виконавця, призначеного користувача та умову рев'ю в режимі self.
- Сценарії 19–26 зіставлено вручну з профілями, бюджетом, безпекою та критеріями завершення.
  Дрібна задача не потребує читання профілів; роль не дає дозволів; самоперевірка не є
  окремим аудитом; недоступний обов'язковий рев'юер залишає незакритий критерій.
  Зауваження рев'юера не дозволяють паралельне редагування або побічні покращення.
- Додаткові агенти не запускалися. Поведінку моделі з новими ролями на реальних задачах
  не виміряно; тести перевіряють валідатор, а ручний розбір — узгодженість інструкцій.
- Нова оцінка ефективності не виставлялася. Наведені нижче 88/100 та скорочення обсягу
  стосуються попередньої версії 2.0.0, а не нового вимірювання 2.1.0.

## Архів перевірки 2.0.0

Дата: 2026-09-06. Початковий Git-контекст: `05fabe4`.
Звіт оцінює комплект документів і його локальний валідатор; він не є сертифікацією моделі.

## Фактичні перевірки

- Windows, PowerShell 7, Python 3.12; стандартна бібліотека, без встановлення залежностей.
- `python -B .agent/maintenance/validate.py --root . --mode template` — `OK` на підготовленому комплекті.
- `python -B -m unittest discover -s .agent/maintenance -p test_validate.py -v` — 17 тестів, `OK`.
  Є позитивні й негативні випадки: пропущений файл, зламане та зовнішнє локальне посилання,
  маркер із суфіксом, невідоме, дубль поля, порожня причина, неузгоджений бюджет, нечислова
  межа, версія, UTF-8 у CLI та встановлення без заміни README продукту.
- Перший запуск виявив помилку кодування українського CLI-виводу у Windows; її виправлено
  явним UTF-8 і перевірено окремим тестом.
- Прочитано змінені правила та зіставлено межі, дозволи, статуси й винятки.
  Пошук залишків старих обов'язкових команд не виявив активних вимог до конкретного стеку.
- Оригінальний текст MIT-ліцензії не змінюється. Людинозрозумілі документи набору українською.

## Ручний розбір сценаріїв

Зіставлено всі 18 рядків [сценаріїв](SCENARIOS.md) із нормативними правилами:

| Сценарії | Джерело очікуваної поведінки | Результат розбору |
|---|---|---|
| 1–4: локальна правка, баг, аналіз, функція | [Межі](../core/WORK_SCOPE.md), [бюджет](../core/WORK_BUDGET.md) | Визначено пов'язані кроки без побічної мети та зайвих дозволів |
| 5: побічний рефакторинг | [Принципи](../core/PRINCIPLES.md) | Старий стиль не дозволяє побічну перебудову |
| 6–8: невдача, commit, відсутня перевірка | [Завершення](../core/DEFINITION_OF_DONE.md), [Git](../core/GIT_WORKFLOW.md), бюджет | Невдалу перевірку не приховано, повтор обґрунтований, відсутнє не названо успішним |
| 9: адаптація шаблону | [Точка входу](../AGENTS.md), [встановлення](INSTALL.md) | Незаповнений шаблон не блокує власну адаптацію |
| 10–11: production і чужі зміни | [Безпека](../core/SECURITY_RULES.md), Git | Збережено межі дозволів і власності |
| 12–13: облік і межа бюджету | Бюджет, завершення | Немає вигаданого вимірювання; `BUDGET LIMIT` не означає `Done` |
| 14: недовірені інструкції | Безпека | Дані інструмента не стають дозволом або новою метою |
| 15–17: уточнення, передача, делегування | Межі, бюджет | Зберігаються виконане, стан процесів і обмеження допоміжних агентів |
| 18: прийнятий ризик | Завершення | Згода не перетворює падіння тесту на успіх |

Розбір не виявив суперечностей у цих сценаріях. Це ручний аналіз документів одним агентом,
а не 18 виконаних задач Astra; автоматичні тести вище перевіряють валідатор, не модель.

## Оцінка: 88 зі 100

Це аргументована експертна оцінка комплекту, не відсоток надійності агента.

| Критерій | Бал | Підстава та обмеження |
|---|---|---|
| Межі задачі й автономність | 24/25 | Є пов'язані кроки, сталі дозволи та заборона побічних цілей; трактування наміру все ще залежить від моделі |
| Економія контексту й повторів | 23/25 | Умовне читання, адресні докази, контроль повторів; немає примусового лічильника витрат |
| Узгодженість, безпека та чесний статус | 18/20 | Усунуто знайдені конфлікти, збережено захист; автоматичної перевірки всіх смислових суперечностей немає |
| Переносимість і адаптація | 14/15 | Відокремлено стек, профіль і Codex; реальні інші агентські середовища не випробувано |
| Докази ефективності | 9/15 | 17 тестів валідатора й 18 ручних сценаріїв; немає порівняльних запусків старих і нових правил на реальних задачах |
| Разом | 88/100 | Оцінка стосується версії документів та інструментів, не майбутніх результатів моделі |

Для підвищення оцінки потрібні вимірювані порівняння на однакових задачах і перевірка
підключення в реальних цільових репозиторіях. Ці експерименти в поточній роботі не виконувались.

## Обсяг початкового читання

Порівняно довжину текстів із нормалізацією переносів рядків до LF, включно з кореневим
AGENTS.md: старий обов'язковий набір — 38127 символів; новий базовий набір із повним
шаблоном контексту — 13855 символів. Скорочення — 63.7%.
Стара вибірка: точка входу, усі сім файлів core та обидва project; нова: точка входу,
WORK_SCOPE, WORK_BUDGET, SECURITY_RULES і PROJECT_CONTEXT. Кореневий файл включено в обидві.
У конкретній задачі додаються потрібні умовні документи, а заповнений контекст матиме інший розмір.
Це вимірювання обсягу інструкцій, не економії токенів або вартості виконання.
